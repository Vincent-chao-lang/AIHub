# Personal AI OS — 你的第二个大脑

> 跨平台 AI 长期记忆系统。无论使用哪个 AI 平台，都带着你的全部上下文。

---

## 一句话描述

**一个浏览器扩展 + 本地后端，自动汇聚你在 ChatGPT、Claude、DeepSeek、Kimi、Gemini 上的所有对话，成为你的个人 AI 记忆系统。**

---

## 解决什么问题

你用多个 AI 平台，但对话完全隔离：

```
痛点 1：碎片化
"关于微服务架构，ChatGPT 聊了方案，Claude 写了代码，DeepSeek 对比了工具——
 但三段对话散落在三个平台，无法拼成完整图景。"

痛点 2：无法检索
"记得问过某个 Prompt 技巧，但在哪个平台问的？只能一个一个翻。"

痛点 3：知识无法关联
"上周学的 Python 装饰器和三个月前聊的 FastAPI 中间件，底层一样。
 但我没意识到，AI 也不知道，等于每次从零开始聊天。"
```

---

## 核心体验

```
在你喜欢的任意 AI 平台页面（ChatGPT / Claude / DeepSeek / Gemini / Kimi）
        │
        ├─ 自动采集 ─── 对话被实时记录，无需手动操作
        ├─ 侧边栏 ──── 点击 🧠 图标，输入话题，检索历史记忆
        └─ 一键注入 ─── 复制上下文，粘贴到对话框
                            │
                            ↓
     AI 带着你的全部历史来回答，不再从零开始认识你。
```

---

## 产品架构（一张图看懂）

```
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ ChatGPT  │ │ Claude   │ │ DeepSeek │ │  Kimi    │ │ Gemini   │   你用的 AI 平台
└────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘
     │            │            │            │            │
     └────────────┴────────────┴────────────┴────────────┘
                              │
                    Chrome 浏览器扩展
                    自动采集 · 流式去重
                              │
                    ┌─────────┴─────────┐
                    │   本地后端服务      │
                    │   FastAPI + SQLite │
                    │   + ChromaDB 向量库 │
                    │                    │
                    │ · 语义搜索          │
                    │ · 自动摘要          │
                    │ · 知识图谱          │
                    │ · 上下文生成        │
                    └─────────┬─────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
        侧边栏随身记忆    Web 可视面板     知识图谱探索
        (任意 AI 页面)  (Timeline/搜索)   (D3.js 力导向图)
```

**一切本地运行——你的数据，只有你能访问。**

---

## 从"记录"到"智能"：四个阶段的数据涌现

| 阶段 | 能力 | 你得到了什么 |
|------|------|------------|
| **1. 统一搜索** | 跨 5 个平台检索，秒级返回 | 不再需要记住在哪个 AI 聊过什么 |
| **2. 自动缝合** | 自动摘要 + 标签 + 项目聚合 | 系统自动聚类你关注的核心领域 |
| **3. 图谱推理** | 向量定位种子 → 图谱遍历发现关联链 | AI 回答时带着你的历史思维关联 |
| **4. 团队沉淀** | 共享后端 + 按人标记 + 按人筛选 | 新人的 AI 对话自动沉淀为团队知识 |

---

## 关键特性

### 采集层

- **无感采集**：不需要切换平台，不需要手动保存，正常聊天就能自动记录
- **5 平台覆盖**：ChatGPT、Claude、DeepSeek、Kimi、Gemini，开箱即用
- **流式去重**：智能识别 AI 的流式输出，不发送不完整的消息

### 智能层

- **语义搜索**：用你说的话去找相似的对话，不是简单的关键词匹配
- **知识图谱**：自动发现对话之间的关联，可视化你的思维网络
- **图谱推理**：从你当前话题出发，沿知识图谱自动发现 N 层关联对话
- **智能截断**：8 档 Token 控制（1K-128K），适配各种模型上下文窗口

### 交互层

- **侧边栏随身**：不离开 AI 对话页面，一键唤起记忆
- **一键注入**：上下文直接复制粘贴到任何 AI 平台
- **Web 面板**：时间线、搜索、项目聚合、知识图谱可视化
- **暗色模式**：自适应系统主题

### 部署层

- **本地优先**：所有数据在本地，零网络依赖，完全隐私
- **团队模式**：也可部署到服务器，多人共享后端，互相查看 AI 决策历史
- **一键启动**：`./start.sh`，前后端同时就绪

---

## 技术亮点

| 亮点 | 说明 |
|------|------|
| **本地 Embedding** | BGE-small 模型本地运行，对话内容不出站 |
| **图谱驱动而非 LLM 驱动** | 关联发现用本地图谱算法，不依赖外部 LLM，零成本零延迟 |
| **纯规则摘要** | TF-IDF 本地提取关键词和标签，不需要 API Key |
| **Token 预算智能分配** | 直接匹配对话优先分配预算，适配不同模型上下文窗口 |
| **插件无状态** | 浏览器扩展只负责采集和转发，所有智能在后端完成 |
| **WAL 模式 SQLite** | 单文件数据库，备份 = 复制文件 |

---

## 目标用户

```
个人开发者    → 所有 AI 对话汇聚一处，跨平台检索，构建个人知识体系
独立研究者    → 不同 AI 讨论同一课题，知识图谱关联，发现深层联系
远程团队      → 共享后端部署，成员 AI 决策过程对团队可见，知识不随人走
隐私敏感者    → 纯本地运行，数据 100% 归自己所有，无需注册，无需联网
```

---

## 路线图

```
✅ Phase 1-7  ─── 全部已完成（采集 · 存储 · 搜索 · 摘要 · 图谱 · 注入 · 团队）
📋 Phase 8    ─── 更多信息源接入（邮件、GitHub、Notion、微信）
📋 Phase 9    ─── Tauri 桌面应用（独立窗口、全局快捷键）
```

---

## 现在就开始

```bash
git clone <repo>
cd AIHub
./start.sh                    # 一键启动
# Chrome → 加载已解压扩展 → 开始使用
```

**你的 AI 对话，从此不再碎片化。**
