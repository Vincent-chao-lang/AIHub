# AI Memory Hub 使用说明

## 安装

```bash
# 1. 启动后端
cd backend && pip install -r requirements.txt && python main.py

# 2. 加载扩展
# Chrome → chrome://extensions → 开发者模式 → 加载已解压 → 选择 extension/ 目录

# 3. 配置（可选）
# 点击 🧠 图标打开侧边栏 → 右上角 ⚙
#   后端地址：默认 http://127.0.0.1:8712（本地），可改为服务器地址
#   用户标识：输入你的名字（团队模式必填）
```

## 日常使用

### 核心流程：侧边栏

```
在任意 AI 平台页面（ChatGPT/Claude/DeepSeek...）
  → 点击浏览器工具栏 🧠
  → 输入你要讨论的话题
  → 选择长度档位（1K/2K/4K/8K/16K/32K/64K/128K）
  → 点击「检索我的记忆」
  → 系统搜索历史 + 图谱发现关联 → 生成上下文
  → 点击「复制上下文」
  → 粘贴到 AI 对话框 → AI 带着你的历史来回答
```

### 长度档位选择

| 档位 | 适用场景 |
|------|---------|
| 1K | 简单问答，快速对话 |
| 2K | 日常讨论（默认） |
| 4K | 深度研究，需要完整背景 |
| 8K | 长上下文模型（Claude/Gemini） |
| 16K | GPT-4 Turbo / DeepSeek-V3 等中长上下文模型 |
| 32K | GPT-4-32K / Kimi 等中等上下文模型 |
| 64K | Gemini 1.5 Flash 等中大型上下文模型 |
| 128K | GPT-4 128K / Claude 200K / Gemini 1.5 Pro 等超长上下文模型 |

当前上下文估算 Token 数会实时显示。

### 上下文自动采集

配置完成后，你在 ChatGPT、Claude、DeepSeek、Kimi、Gemini 上的所有对话会自动记录，无需手动操作。

## Web 面板（可选）

访问 `http://localhost:5173` 可使用更多功能：

| 页面 | 功能 |
|------|------|
| 时间线 | 按日期浏览所有对话，按平台/用户筛选 |
| 项目 | 自动按主题聚合的对话分组 |
| 上下文 | 和侧边栏相同功能，适合深度搜索 |
| 图谱 | 可视化你的知识关联网络 |

## 团队模式

```
1. 后端部署到共享服务器
2. 每人安装扩展 → 设置页填入：
   - 后端地址：https://your-server.com
   - 用户标识：zhang-san / li-si
3. 正常使用 → 消息自动标记用户
4. 搜索时可查看到团队其他人的 AI 对话
5. 新人入职 → 搜索关键词 → 看到前辈的完整 AI 决策过程
```

## 一键启动

```bash
./start.sh      # 同时启动前后端
Ctrl+C          # 关闭所有服务
```

## 常见问题

**Q: 侧边栏点「检索我的记忆」没反应？**
A: 确认后端已启动（`./start.sh`），检查设置页 → 后端地址是否正确 → 连接测试是否绿点。

**Q: 页面上的 AI 对话没被自动采集？**
A: 刷新扩展（chrome://extensions → 🔄），打开 F12 Console 看是否有 `[AI Memory Hub]` 日志。

**Q: 数据库在哪？数据怎么备份？**
A: `backend/aihub.db`（SQLite），直接复制这个文件即可备份。向量索引在 `backend/.chromadb/`。

**Q: 如何切换到服务器模式？**
A: 设置页修改后端地址 → 保存 → 绿点即通。
